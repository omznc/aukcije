---
import type { Listing } from '../../schema.ts';
import { formatMoney, daysUntil } from '../lib/data.ts';
import { headline } from '../lib/headline.ts';
import { metaLine, discount, formatPercent, urgency, daysLabel } from '../lib/stats.ts';

interface Props {
  listing: Listing;
  /** Off on a court's own page, where every row names the same court. */
  showCourt?: boolean;
  /** The home table draws the discount as a bar as well as a number. */
  bar?: boolean;
  attrs?: Record<string, string>;
}
const { listing: l, showCourt = true, bar = false, attrs = {} } = Astro.props;

const days = daysUntil(l.saleDate);
const off = discount(l);

const URGENCY_COLOR = {
  due: 'text-brick',
  soon: 'text-rust',
  later: 'text-ink-mid',
  past: 'text-dim',
} as const;
---

<a
  href={`/oglas/${l.id}/`}
  {...attrs}
  class:list={[
    'grid grid-cols-[70px_1fr] items-start gap-x-4 gap-y-2 border-b border-hair py-3.5 hover:bg-[#faf8f4] md:items-center md:gap-y-0',
    'gut',
    showCourt
      ? 'md:grid-cols-[88px_1fr_160px_120px_120px_132px]'
      : 'md:grid-cols-[88px_1fr_120px_120px_132px]',
  ]}
>
  <div>
    <div class:list={['font-mono text-[12.5px] font-bold leading-none', URGENCY_COLOR[urgency(days)]]}>
      {daysLabel(days)}
    </div>
    <div class="mt-1 font-mono text-[10.5px] leading-none text-faint">
      {l.saleDate.slice(8, 10)}.{l.saleDate.slice(5, 7)}.
    </div>
  </div>

  <div class="md:pr-5">
    <div class="text-[14px] font-semibold leading-snug">{headline(l)}</div>
    <div class="mt-[3px] text-[11.5px] leading-snug text-dim">{metaLine(l)}</div>
  </div>

  {
    showCourt && (
      <div class="col-start-2 text-[11.5px] leading-snug text-ink-mid md:col-start-auto md:pr-3">
        {l.court}{' '}
        <span class="whitespace-nowrap border border-edge px-1 py-0.5 font-mono text-[9.5px] font-medium leading-none">
          {l.entity}
        </span>
      </div>
    )
  }

  <div
    class="col-start-2 flex flex-wrap items-baseline gap-x-4 md:col-start-auto md:contents"
  >
    <span class="font-mono text-[13px] leading-none text-dim md:text-right">
      {formatMoney(l.appraisedValue) ?? '—'}
    </span>
    <span class="font-mono text-[14px] font-bold leading-none md:text-right">
      {formatMoney(l.startingPrice) ?? <span class="font-normal text-ghost">—</span>}
    </span>
    <span class="md:text-right">
      <span
        class:list={[
          'font-mono text-[13px] font-bold leading-none',
          off === null ? 'text-ghost' : off > 0 ? 'text-pine' : 'text-dim',
        ]}
      >
        {off === null ? 'nema početne' : formatPercent(off)}
      </span>
      {
        bar && (
          <span class="mt-1.5 hidden h-1 bg-track md:block">
            <span
              class="ml-auto block h-1 bg-pine"
              style={`width:${Math.max(0, Math.min(100, Math.round((off ?? 0) * 100)))}%`}
            />
          </span>
        )
      }
    </span>
  </div>
</a>
